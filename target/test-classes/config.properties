# Configuración base - será sobrescrita por el mock server
base.url=http://localhost:8080
api.base.path=/api/todos
browser=chrome
headless=true
implicit.wait=10
explicit.wait=15
# Puerto del mock server
mock.server.port=8080
